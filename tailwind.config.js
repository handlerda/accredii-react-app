//tailwind.config.js
// This file imports all of the required plugins, you can also customize colors, sizing, themes, etc here (I recommend looking into it)
module.exports = {
  plugins: [
    require("tailwindcss"),
    require("autoprefixer"),
    require("@tailwindcss/ui"),
    require("@tailwindcss/forms"),
  ],
};
